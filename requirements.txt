# Core
torch>=2.3.0
transformers>=4.44.0
datasets>=2.20.0
accelerate>=0.33.0
tokenizers>=0.19.0

# Training speedups (H20 / CUDA only -- skip on Mac)
# flash-attn>=2.5.0   ; install on the H20 box: pip install flash-attn --no-build-isolation

# Utilities
numpy
tqdm
sentencepiece
